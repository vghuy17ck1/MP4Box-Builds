# MKV fixture

The fixture is generated in CI from a legally redistributable synthetic H.264/AAC source with the system FFmpeg tool. Set MP4BOX_MKV_FIXTURE to provide a checked-in fixture for offline runs.

